{
  "name": "@beppobert/ts-math",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --watch 'src/*.ts' --exec analyze-trace ./tracing"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript/analyze-trace": "^0.10.1",
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
